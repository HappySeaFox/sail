# This file is used for source distributions (sdist) only.
# Binary wheel content is controlled by pyproject.toml [tool.setuptools.package-data]

# Include documentation
include README.md
include LICENSE

# Include configuration files needed for sdist build
include pyproject.toml
include setup.py
include CMakeLists.txt

# Include all C++ source files for building from source (Python bindings)
recursive-include src *.cpp

# Note: SAIL source files (CMakeLists.txt, cmake/, src/sail/, etc.) are added
# automatically by CustomSDist.make_release_tree() during sdist creation.

# Exclude build artifacts and cache
global-exclude *.so.*
