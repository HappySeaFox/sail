[build-system]
requires = [
    "setuptools>=45",
    "wheel",
    "pybind11>=2.10.0",
    "cmake>=3.12",
    "numpy>=1.20.0",
]
build-backend = "setuptools.build_meta"

[project]
name = "sailpy"
version = "@SAIL_VERSION@"
description = "Python bindings for SAIL - Squirrel Abstract Imaging Library"
readme = {file = "README.md", content-type = "text/markdown"}
authors = [
    {name = "Dzmitry Baryshau", email = "dima8w@gmail.com"}
]
license = "MIT"
keywords = [
    "image",
    "loading",
    "decoding",
    "encoding",
    "jpeg",
    "png",
    "webp",
    "avif",
    "tiff",
    "gif",
    "bmp",
    "heif",
    "graphics",
]
classifiers = [
    "Development Status :: 5 - Production/Stable",
    "Intended Audience :: Developers",
    "Intended Audience :: Science/Research",
    "Programming Language :: C",
    "Programming Language :: C++",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
    "Topic :: Multimedia :: Graphics",
    "Topic :: Multimedia :: Graphics :: Graphics Conversion",
    "Topic :: Scientific/Engineering :: Image Processing",
    "Topic :: Software Development :: Libraries :: Python Modules",
    "Operating System :: OS Independent",
]
requires-python = ">=3.10"
dependencies = [
    "numpy>=1.20.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0",
]

[project.urls]
Homepage = "https://sail.software"
Repository = "https://github.com/HappySeaFox/sail"
Issues = "https://github.com/HappySeaFox/sail/issues"

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]

[tool.setuptools]
include-package-data = true

[tool.setuptools.packages.find]
include = ["sailpy*"]

[tool.setuptools.package-data]
sailpy = ["*.dll", "*.pyd", "*.so", "*.so.*", "_version.py", "py.typed"]
"sailpy.examples" = ["*.py", "*.md"]
